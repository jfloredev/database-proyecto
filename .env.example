# Variables de entorno para Docker Compose (local)
# Copia este archivo como .env y ajusta los valores para producción

# Database
DB_HOST=postgres
DB_NAME=db_mifarma
DB_USER=postgres
DB_PASSWORD=12345678

# API URL para frontend (ajustar en producción con IP pública o dominio)
REACT_APP_API_BASE_URL=http://localhost:8080
